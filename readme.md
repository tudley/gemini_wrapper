Project Vision
Goal

Build a reusable AI backend that can communicate with a frontend and safely interact with application state through backend-defined tools.

The long-term target is an AI-powered Strava coach, but the initial implementation uses a simple todo list to validate the architecture.